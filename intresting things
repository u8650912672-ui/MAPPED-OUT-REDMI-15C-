Intresting things i can find


  The engineering scatter file (MT6768_Android_scatter.txt) lists physical partitions. Preloader runs from internal SRAM (usually 0x00100000–0x00200000). The DA payload address is 0x201000 (writable), but the need of function pointer is called after the write, or a spot in code that checks DA authentication.

    The error code 0x1d18 is set at 0x3fd6:
    text

      0x3fd6   movw fp, 0x1d18

    If we overwrite that instruction with movw fp, 0x0000, the preloader will return success for any upload, bypassing DAA/SLA. We can try writing to that exact address.
